# dinit Brewfile — greyZ / macOS Tahoe (Apple Silicon)
# Formulae only. Casks live behind --with-casks in dinit.sh.

# --- version control + github ---
brew "git"
brew "git-lfs"
brew "gh"

# --- agent / script staples ---
brew "jq"
brew "yq"
brew "ripgrep"
brew "fd"
brew "fzf"
brew "bat"
brew "eza"
brew "tree"
brew "wget"
brew "rsync"
brew "coreutils"
brew "gnu-sed"
brew "shellcheck"

# --- compile / native deps (rust, node native modules, wasm) ---
brew "cmake"
brew "pkgconf"
brew "openssl@3"
brew "libiconv"

# --- runtimes + task runners ---
brew "mise"          # node + python versions, no brew-node fight
brew "bun"           # vite / astro speedrun
brew "just"
brew "direnv"
brew "zoxide"
brew "watchexec"

# optional docker runtime (installed only with --with-casks)
# cask "orbstack"
